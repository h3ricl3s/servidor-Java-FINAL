server.port = ${PORT:8080}

spring.application.name=servidorlocal

spring.datasource.url=${DB_URL:jdbc:h2:file:./data/servidorlocal}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}

spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=${DB_DRIVER:org.h2.Driver}
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.H2Dialect}

# Desativar prepared statements nomeados no JDBC para compatibilidade com PgBouncer / Supabase Pooler (porta 6543)
spring.datasource.hikari.data-source-properties.prepareThreshold=0


spring.h2.console.enabled=true

server.max-http-header-size=128KB
server.tomcat.max-http-form-post-size=2MB

# Configurações do servidor de Email (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hericlesmilton@gmail.com
spring.mail.password=rmypaxohhmpoqdek

# Configurações avançadas de segurança exigida pelo Gmail
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

jwt.secret=${JWT_SECRET:jnvbjvlgjvguvyugvugbvugbulgy8ss}